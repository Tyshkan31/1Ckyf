// Проверяет и показывает новые оповещения
Процедура ПроверитьНовыеОповещения() Экспорт
    
    МассивОповещений = ОповещенияСервер.ПолучитьНепрочитанныеОповещения();
    
    Для Каждого Оповещение Из МассивОповещений Цикл
        
        ПоказатьОповещение(Оповещение);
        
        ОповещенияСервер.ОтметитьКакПрочитанное(
            Оповещение.Пользователь, 
            Оповещение.ВремяСобытия, 
            Оповещение.УникальныйКлюч
        );
        
    КонецЦикла;
    
КонецПроцедуры

Процедура ПоказатьОповещение(ДанныеОповещения)
    
    Заголовок = ДанныеОповещения.Заголовок;
    ТекстПояснения = ДанныеОповещения.ТекстОповещения;
    НавигационнаяСсылка = ДанныеОповещения.НавигационнаяСсылка;
    УникальныйКлюч = ДанныеОповещения.УникальныйКлюч;
    
    Если ПустаяСтрока(НавигационнаяСсылка) Тогда
        НавигационнаяСсылка = "";
    КонецЕсли;
    
    ПоказатьОповещениеПользователя(
        Заголовок,
        НавигационнаяСсылка,
        ТекстПояснения,
        БиблиотекаКартинок.Информация,
        СтатусОповещенияПользователя.Важное,
        УникальныйКлюч
    );
        
КонецПроцедуры