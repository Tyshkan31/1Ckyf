Процедура Тест() Экспорт

    ТекущийПользовательСправочник = УправлениеОповещениями.ПолучитьТекущегоПользователя();

    Если ЗначениеЗаполнено(ТекущийПользовательСправочник) Тогда

        Сообщить("✓ Пользователь найден: " + Строка(ТекущийПользовательСправочник));
        Сообщить("  Наименование: " + ТекущийПользовательСправочник.Наименование);

    Иначе

        Сообщить("✗ Пользователь НЕ найден!");
        
        // Показываем что ищем
        УстановитьПривилегированныйРежим(Истина);
        ТекПользИБ = ПользователиИнформационнойБазы.ТекущийПользователь();
        Сообщить("  ИдентификаторПользователяИБ для поиска: " + Строка(ТекПользИБ.УникальныйИдентификатор));
        УстановитьПривилегированныйРежим(Ложь);
                       	
    КонецЕсли;

КонецПроцедуры                                     	